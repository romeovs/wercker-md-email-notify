box: nahody/nodejs
build:
  steps:
    - script:
        name: install md-mailer
        code: |
          mkdir -p node_modules
          npm install md-mailer
    - shellcheck
    # - validate-wercker-step
