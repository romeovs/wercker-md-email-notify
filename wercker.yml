# we need node
box: wercker/default

build:
  steps:
    - install-packages:
        packages: npm
    - script:
        name: install md-mailer globally
        code: |
          sudo npm install -g md-mailer
    - shellcheck
    # - validate-wercker-step
