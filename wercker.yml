box: nahody/nodejs
build:
  steps:
    - script:
        name: install md-mailert
        code: |
          mkdir -p node_modules
          npm install md-maileu
    - shellcheck
    # - validate-wercker-step
